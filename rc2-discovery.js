(()=>{
  'use strict';
  const esc=value=>String(value??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
  const finite=n=>Number.isFinite(Number(n));
  const rad=x=>x*Math.PI/180;
  const distanceKm=(a,b)=>{const R=6371,dLat=rad(Number(b.lat)-Number(a.lat)),dLon=rad(Number(b.lng)-Number(a.lng));const s=Math.sin(dLat/2)**2+Math.cos(rad(Number(a.lat)))*Math.cos(rad(Number(b.lat)))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.min(1,Math.sqrt(s)))};
  const categoryGroup=p=>{const s=`${p.category||''} ${p.subcategory||''}`.toLowerCase();if(/cây xăng|xăng dầu|petrol|fuel/.test(s))return'Cây xăng';if(/trạm dừng|điểm dịch vụ|rest stop/.test(s))return'Trạm dừng';if(/ăn|quán ăn|ẩm thực|food/.test(s))return'Ăn uống';if(/lưu trú|khách sạn|nhà nghỉ|villa|resort|hotel/.test(s))return'Lưu trú';if(/tham quan|vui chơi|check.?in|du lịch|tâm linh|di tích|bảo tàng/.test(s))return'Tham quan';return'Khác'};
  const inferStage=p=>{if(p.corridorStage)return p.corridorStage;const s=`${p.address||''} ${p.note||''}`.toLowerCase();if(/hà tiên|ha tien/.test(s))return'Hà Tiên';if(/kiên lương|kien luong/.test(s))return'Kiên Lương';if(/rạch giá|rach gia/.test(s))return'Rạch Giá';if(/mỹ tho|my tho/.test(s))return'Mỹ Tho';if(/bến lức|ben luc/.test(s))return'Bến Lức';return'Khác'};
  const canRankGps=p=>p.gpsRankEligible!==false&&finite(p.lat)&&finite(p.lng);
  const mapDest=p=>canRankGps(p)?`${Number(p.lat)},${Number(p.lng)}`:(p.address||p.name||'');
  const directions=p=>`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(mapDest(p))}`;
  const mapUrl=p=>`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(mapDest(p))}`;
  let active=localStorage.getItem('d1-discovery-category')||'Tất cả';
  let stage=localStorage.getItem('d1-discovery-stage')||'Tất cả';
  let radius=Number(localStorage.getItem('d1-discovery-radius'))||50;
  const currentOrigin=()=>lastFix||JSON.parse(localStorage.getItem('d1-last-gps')||'null');
  function discoveryList(q=''){const origin=currentOrigin();return pois().map(p=>({...p,group:categoryGroup(p),stage:inferStage(p),distance:origin&&canRankGps(p)?distanceKm(origin,p):null})).filter(p=>active==='Tất cả'||p.group===active).filter(p=>stage==='Tất cả'||p.stage===stage).filter(p=>!q||JSON.stringify(p).toLowerCase().includes(q)).filter(p=>p.distance===null||p.distance<=radius).sort((a,b)=>{if(a.distance===null&&b.distance===null)return Number(b.verified)-Number(a.verified);if(a.distance===null)return 1;if(b.distance===null)return-1;if(a.distance!==b.distance)return a.distance-b.distance;return Number(b.verified)-Number(a.verified)})}
  window.renderDiscovery=function(q=''){const origin=currentOrigin();const list=discoveryList(q);const cats=['Tất cả','Ăn uống','Trạm dừng','Cây xăng','Lưu trú','Tham quan'];const stages=['Tất cả','Bến Lức','Mỹ Tho','Rạch Giá','Kiên Lương','Hà Tiên'];const cards=list.length?list.map(p=>`<article class="panel"><span class="tag">${esc(p.group)}</span><span class="tag">${esc(p.stage)}</span>${p.verified?'<span class="tag good">✓ Đã xác nhận</span>':'<span class="tag warn">Chưa xác minh</span>'}<h3>${esc(p.name)}</h3><div class="meta">${esc(p.category||'')} ${p.subcategory?`· ${esc(p.subcategory)}`:''}<br>${p.gpsRankEligible===false?'Chờ xác minh tọa độ':p.distance===null?'Chưa tính được khoảng cách':`${p.distance<1?Math.round(p.distance*1000)+' m':p.distance.toFixed(1)+' km'} từ vị trí hiện tại`}<br>Tuyến: ${esc(p.routeId||'—')}</div>${p.address?`<p class="meta">${esc(p.address)}</p>`:''}${p.note?`<p>${esc(p.note)}</p>`:''}<div class="actions"><a class="pill primary" href="${directions(p)}" target="_blank" rel="noopener">Dẫn đường</a><a class="pill" href="${mapUrl(p)}" target="_blank" rel="noopener">Mở bản đồ</a></div></article>`).join(''):'<div class="panel">Chưa có địa điểm phù hợp bộ lọc. Hãy đổi chặng/danh mục hoặc tăng bán kính.</div>';document.getElementById('main').innerHTML=`<h2>Khám phá quanh tôi</h2><div class="notice">${origin?`Đang dùng vị trí GPS gần nhất ±${Math.round(origin.accuracy||0)} m.`:'Chưa có GPS. Bật Vị trí hiện tại để sắp xếp theo khoảng cách thực.'}</div><div class="actions">${cats.map(c=>`<button class="${c===active?'primary':''}" onclick="setDiscoveryCategory('${c}')">${c}</button>`).join('')}</div><div class="actions"><label class="meta">Chặng <select id="discoveryStage" onchange="setDiscoveryStage(this.value)">${stages.map(s=>`<option value="${s}" ${stage===s?'selected':''}>${s}</option>`).join('')}</select></label><label class="meta">Bán kính <select id="discoveryRadius" onchange="setDiscoveryRadius(this.value)"><option value="10" ${radius===10?'selected':''}>10 km</option><option value="20" ${radius===20?'selected':''}>20 km</option><option value="30" ${radius===30?'selected':''}>30 km</option><option value="50" ${radius===50?'selected':''}>50 km</option><option value="100" ${radius===100?'selected':''}>100 km</option></select></label><span class="meta">${list.length} điểm phù hợp</span></div><div class="grid" style="margin-top:10px">${cards}</div>`};
  window.setDiscoveryCategory=c=>{active=c;localStorage.setItem('d1-discovery-category',c);renderDiscovery(document.getElementById('search')?.value.toLowerCase().trim()||'')};
  window.setDiscoveryStage=s=>{stage=s;localStorage.setItem('d1-discovery-stage',s);renderDiscovery(document.getElementById('search')?.value.toLowerCase().trim()||'')};
  window.setDiscoveryRadius=v=>{radius=Number(v)||50;localStorage.setItem('d1-discovery-radius',String(radius));renderDiscovery(document.getElementById('search')?.value.toLowerCase().trim()||'')};
  const priorRender=window.render;window.render=function renderWithDiscovery(){const q=document.getElementById('search')?.value.toLowerCase().trim()||'';document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));document.querySelectorAll('[data-nav]').forEach(b=>b.classList.toggle('active',b.dataset.nav===view));if(view==='discovery'){renderDiscovery(q);updateHero();return}priorRender()};document.querySelectorAll('[data-view="discovery"],[data-nav="discovery"]').forEach(b=>{b.onclick=()=>{view='discovery';window.render()}});
})();
